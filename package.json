{
  "name": "@occumed/map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:style": "node scripts/prepare-reference.mjs",
    "validate:export": "node scripts/check-export.mjs",
    "check": "npm run validate:export",
    "dev": "npm run validate:export && npm run prepare:style && vite",
    "build": "npm run validate:export && npm run prepare:style && vite build",
    "preview": "vite preview",
    "start": "node server.mjs"
  },
  "dependencies": {
    "mapbox-gl": "3.26.0"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
