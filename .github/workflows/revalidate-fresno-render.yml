name: Revalidate Fresno PMTiles render

on:
  workflow_dispatch:

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fresno render validation is paused until the worldwide manifest is complete."
