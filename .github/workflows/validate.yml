name: Validate Occu-Med open basemap

on:
  workflow_dispatch:

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Open-basemap validation is paused until the worldwide PMTiles release is complete."
